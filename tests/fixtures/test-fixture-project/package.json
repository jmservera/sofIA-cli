{
  "name": "test-fixture-project",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=json"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
